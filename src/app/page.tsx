import LandingPage from "./_landing/LandingPage";

export const metadata = {
  title: "LearnMate — Your AI study buddy that actually listens",
  description:
    "Free, voice-first AI study assistant. Talk, chat, take notes, generate quizzes, polish research — all in your browser, zero cost.",
};

export default function Page() {
  return <LandingPage />;
}
